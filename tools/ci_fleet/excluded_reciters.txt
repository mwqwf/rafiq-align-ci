# ⛔ قرّاءٌ قِيسوا وأُغلقوا — لا يُنفَق عليهم حكمٌ ولا محاذاة.
#
# **لماذا وُجد هذا الملفّ (قِيس 2026-09-12 ‏01:4xZ):** كان الإخراجُ مكتوباً في
# تعليقات `reciters_remaining.tsv` وحدَها — **والنبّاضُ لا يقرأ التعليقات**.
# فوظيفةُ `gate_and_promote` تُبوّب كلَّ مرشَّحِ مسرحٍ لقارئٍ **غيرِ منشور**
# (`salts<4` · ليس في الطيران · غيرُ منشور) ولا تعرف أنّ القارئَ مُخرَجٌ بقياس.
# ⇒ **فأُنفقت عشرُ تشغيلاتٍ على `janaini_qalun` وحدَه في واحدٍ وعشرين دقيقة**:
#   `426c193b` ‏01:12Z (‏مطالع + `k1…k4`) ثمّ `ffe55710` ‏01:33Z (‏خمسٌ أخرى) —
#   وهو **مُخرَجٌ بقياسٍ مكتوب**: «ابتلاعٌ حتميّ: بصمتان متتاليتان 9 تتابعات
#   و0 فجوة نظيفة». وكلُّ إعادةِ محاذاةٍ تُنتج بصمةً جديدةً فتبدأ الخمسُ من جديد.
#
# ⛔ **والحدُّ ليس مسألةَ هدرٍ فحسب بل مزاحمةٌ على الحصّة:** الموجةُ الجاريةُ
#    `34662493606` فيها **خمسُ شاردةٍ لقرّاءَ مُخرَجين** (‏kholti 2 · h_saleh 4 ·
#    saad 5 · mhsny 7 · janaini 8) ⇒ **~25 تشغيلةً** كانت ستُنفق على محسومين،
#    بينما **الستّةُ النافعون** ينتظرون أحكامَهم في حصّةٍ سعتُها عشرون.
#
# **الشكل:** معرّفُ القارئ (‏كما في `reciters_*.tsv` وكما في اسم البصمة) ثمّ
# `#` ثمّ **السببُ بأرقامه المقيسة**. ⛔ ولا يُضاف سطرٌ بلا رقمٍ قِيس.
# ⛔ **ولا يُخرَج من ينتظر إذنَ المالك** (`rabbani_warsh` · `iraoui_warsh`) —
#    مانعُهما إذنُ إسقاطٍ لا رداءةُ محاذاة، وحكمُهما عَبَر (1.5% · 2.3%).

# ── رداءةُ محاذاةٍ مقيسة (‏العتبة 5% ولا تُلمس) ───────────────────────────
f_khamery       # 17.75% — وبـ16.0% في قياس 09-08؛ ac=256 لم ينظّف تفريغَه
mhsny           # 17.12% + ابتلاع
bilal           # 8.89% من 1800 — وفشلُ «لم تُحلّ سورةٌ واحدة» بـac=256
qasm            # 6.38% بعد القصّ
hatem           # 5.67% من 1200
h_abudalal      # 5.5%
kholti_warsh    # 4.75% بثمانية ملوح — بين 4.5 و5 فلا يُحسم بالعيّنة (يلزم n>19500)

# ── ⏳ تحت إعادةِ اختبارٍ جاريةٍ — مُخرَجان مؤقّتاً من الإخراج (2026-09-12 ‏13:3xZ) ──
# ⛔⛔ **العطبُ المُصلَح هنا: الفلترُ يُخرِج بالمعرّف لا بالمحرّك — فيُسقط صامتاً
#     نتيجةَ التجربة التي نُنفق عليها الحوسبةَ الآن.** `excluded` يُقرأ في
#     `keepalive.yml` في فلتر `need` وحدَه (`_who(k)[1] not in excluded`)،
#     **ولا يعرف بأيّ `whisper_ac` أُنتجت البصمة**. فحكمُ 09-08 قِيس بـ**ac=0**،
#     وتجربةُ الهلوسة تُنتج بصمةً بـ**ac=256** — فلو بقي الاسمُ مُخرَجاً
#     **لَما بوَّب النبّاضُ البصمةَ الجديدةَ أبداً** وذهبت الموجةُ هباءً.
#   • `shaksh`         — `34689048397` (‏reciters_halluc_ac256.tsv · بدأ 10:40Z · سقفُه ~16:30Z)
#   • `chahboun_warsh` — `34688288330` (‏reciters_base1.tsv · بدأ 10:22Z · سقفُه ~16:12Z)
# ✅ **والثمنُ قِيس صفراً بالطرفين** (‏قاعدة «يُختبر الفلترُ بالطرفين»):
#   أحدثُ بصمةٍ لكلٍّ منهما **ممْلوحةٌ فوق الأربعة** — `shaksh.b2fb8694` ‏09-08
#   بـ**9 ملوح** و`chahboun_warsh.b4733497` ‏09-11 بـ**12 ملحاً** ⇒ شرطُ
#   `salts<4` يمنع تبويبَهما، **فلا تُنفَق تشغيلةٌ واحدةٌ حتى تنزل البصمةُ
#   الجديدة**. وحينئذٍ تبويبُها هو المطلوبُ عينُه.
# ⛔ **وحكمُ الستّة معلَّقٌ عليهما:** إن نزل `shaksh` عن 5.7% فالهلوسةُ سببُ
#   رسوبِ الستّة ⇒ تُعاد محاذاتُهم (‏حتى +6). وإن رسب بـac=256 **فيُعاد سطرُه
#   هنا بالرقم الجديد** — ولا يُعاد بلا رقمٍ قِيس.
# chahboun_warsh  # 5.08% من 2400 (‏و5.2% مجمَّعاً بثمانية ملوح) — مُعلَّقٌ حتى ينتهي 34688288330
# shaksh          # 4.91% من 2200 — والمجمَّعُ الحقيقيّ 5.7% — مُعلَّقٌ حتى ينتهي 34689048397

# ── أعطابٌ بنيويّةٌ حتميّة (‏لا تُصلحها إعادةُ محاذاة) ────────────────────
janaini_qalun   # ابتلاعٌ حتميّ: بصمتان متتاليتان 9 تتابعات و0 فجوة نظيفة
saad            # 4.33% لكن ابتلاعٌ في 49 تتابعاً من 53
h_saleh         # عيّنةٌ 0.12% بلا برهان تنزيل (0/114) وإعادةُ المحاذاة تُنقصه إلى 5791

# ── مبتوتون بقياسٍ سابق (‏منشورون، فالحارسُ يكفيهم — ويُثبتون هنا احتياطاً) ─
qeryo_qalun     # 5.0% من ثماني عيّنات = العتبةُ نفسُها؛ زيادةُ العيّنة رفعته ولم تخفضه
3siri           # مانعُه س9 (129 آية) والتغطيةُ بعد الإسقاط 97.93% < 98%
