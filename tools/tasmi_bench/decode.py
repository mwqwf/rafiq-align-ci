# -*- coding: utf-8 -*-
"""🎚️ **فكُّ ترميزٍ صوتيٍّ لا يُقصي سليماً** — نداءٌ واحدٌ لكلِّ أدوات التسميع.

⚠️ **لِمَ وُجد هذا الملفّ** (تشخيصُ جلسة الفهرسة `github-17`، 2026-09-08، مقيسٌ على البايتات):
ملفّاتُ mp3quran كثيراً ما تحمل غلافاً في وسم ID3، وقد يعلن إطارُ الغلاف `APIC` طولاً
**يتجاوز الوسمَ الذي يحويه** (‏11,835 بايت داخل وسمٍ يعلن 6,166). فيجري مُستكشِفُ ffmpeg
خارجَ الوسم إلى الصوت ويفكّ بايتاته صورةً، **فلا يسجّل تيّارَ صوتٍ قطّ** — والصوتُ سليمٌ
تماماً (أوّلُ إطارٍ عند 6176 مزامنتُه `ff fb`).

والعطبُ في **الاستكشاف** لا في الإخراج، ولذلك **`-vn` وحدَه لا يكفي**: هو صحيحٌ في نفسه
(دالّةُ صوتٍ لا تريد صورةً أبداً) وينقّي التشخيص، لكنّ المخرَجَ يبقى خالياً.
والعلاجُ `-f mp3`: يفرض المُفكِّك فيمسح بحثاً عن المزامنة ويتخطّى الوسمَ المعطوب.

⛔ **ولا يُفرَض في المسار الأوّل:** الدالّةُ نفسُها تخدم wav وogg، وفرضُ mp3 عليها يكسرها.
فهو **احتياطٌ بعد الفشل** لا بديلٌ عن المحاولة الأولى، ولا يُجرَّب إلا على مدخلٍ mp3.

⛔ **وشرطُ النجاح ليس شفرةَ الخروج وحدَها:** الحالةُ الموصوفة تُخرج ملفّاً فارغاً بشفرةٍ
غير صفريّة أحياناً وبصفريّةٍ أحياناً، فيُشترط **مخرَجٌ موجودٌ غيرُ فارغ**.
"""
import os
import subprocess


class DecodeError(RuntimeError):
    pass


def run_decode(cmd, src, dst):
    """ينفّذ أمر ffmpeg، فإن أخفق ومصدرُه mp3 أعاده بـ`-f mp3` قبل `-i`.

    `cmd` قائمةٌ كاملة تحوي `-i src` و`dst` في آخرها.
    يعيد `dst` عند النجاح، ويرفع [DecodeError] وقد جُرّب الاحتياط.
    """
    def attempt(c):
        r = subprocess.run(c, capture_output=True, text=True)
        ok = r.returncode == 0 and os.path.exists(dst) and os.path.getsize(dst) > 44
        return ok, (r.stderr or "")[-300:]

    ok, err = attempt(cmd)
    if ok:
        return dst

    if str(src).lower().endswith(".mp3") and "-i" in cmd:
        i = cmd.index("-i")
        forced = cmd[:i] + ["-f", "mp3"] + cmd[i:]
        ok2, err2 = attempt(forced)
        if ok2:
            return dst
        raise DecodeError(f"فكُّ الترميز أخفق مرّتين — {src}\n  الأولى: {err}\n  بـ-f mp3: {err2}")

    raise DecodeError(f"فكُّ الترميز أخفق — {src}\n  {err}")
