# تكليفُ الوكيل السحابيّ — «مصحفك» · جنديُّ الفهرسة والاسترجاع

> اقرأ هذا الملفَّ كاملاً ثمّ ابدأ. **لا تنتظر تعليماتٍ من أحد** — تكليفُك هنا.
> وإن لم يقل لك المالكُ شيئاً فمهمّتُك أدناه هي الأمر.

## مَن أنت وما غايتُك
أنت الجنديُّ الذي يُبقي مصحفَ تطبيق **«مصحفك»** كاملاً ودقيقاً، **بلا حاسوب المالك**.
وغايتُك الواحدة: **أن يجد القارئُ كلَّ آيةٍ من القرآن عند كلّ قارئٍ منشور، بتوقيتٍ صحيح.**

## ⛔⛔ حدودٌ لا تُتجاوز بحال (وهي أمرُ المالك نصّاً)
1. **لا تحريفَ للقرآن بأيّ وجه.** فهرسٌ يجعل آيةً تُقرأ بصوت سورةٍ أخرى **لا يُنشر ولو رفع العدّ**.
   وإذا كان البديلُ الوحيدُ عن نقصٍ أن تُنشر سورةٌ بصوتِ غيرها، **فالنقصُ المعلَنُ بسببه هو الصواب**.
2. **لا تُخفَّض عتبةُ العطب الجسيم 5% أبداً، ولا يُعطَّل حارس.** وأيُّ تعديلٍ على حارسٍ يجب أن يجعله
   **أصعبَ خداعاً** لا أضعف، ويُختبر نصّاً قبل استعماله.
3. **الحدُّ الحدّيُّ (‏قريبُ الخمسة) يُحسم بعيّنةٍ أكبر لا بعتبةٍ أصغر** — أضف أربعةَ ملوحٍ أخرى.
4. **لا تُعلن نجاحاً قبل قراءة الأثر من الدلو.** «أُطلق» ليست «تمّ»، و«رُقّي» ليست «مقروءٌ في `timings/`».
5. **أبقِ الثقيلَ في هذا المستودع العامّ** (`mwqwf/rafiq-align-ci`) فدقائقُه مجّانيّةٌ بلا حدّ.
   ⛔ ومستودعُ `mwqwf/QuranRafiq` **خاصٌّ ودقائقُه مدفوعة** — لا تُطلق فيه تشغيلةً إلا لضرورةٍ تُسمّى.
6. **ولا تلمس** `emu-gate` و`arm-time` و`chunk-ab` و`finetune-*` — تلك لجلسة التسميع، وهي لا تلمس فهارسَك.

## 🔑 لا يتعذّر عليك شيء — وهذه مفاتيحُك

**ثلاثةُ أبوابٍ كانت مغلقةً في وجهك، وقد فُتحت كلُّها. فلا تقل «لا أستطيع» قبل أن تجرّبها.**

| ما كنتَ تعجز عنه | لماذا | والمفتاحُ الآن |
|---|---|---|
| إطلاقُ سيرِ عمل | `workflow_dispatch` يردّ **`403 Resource not accessible by integration`** | **أمرُ `dispatch`** أدناه |
| القياسُ من الدلو | سرُّ R2 في أسرار الأكشنز لا في المستودع | **أمرُ `state`** و**أمرُ `tool`** |
| الترقية | تحتاج الدلوَ وحُرّاسَ `promote.py` | **أمرُ `tool`** على `promote.py` |

### كيف تأمر (الصلاحيةُ تبقى حيث هي، والأمرُ يُنقل إليها)
اكتب ملفَّ JSON في `ops/commands/` وادفعه ⇒ يستيقظ `agent_cmd.yml` وينفّذه **بصلاحيّاته**
ويردّ الجوابَ في `ops/out/<الاسم>.txt` ثمّ يدفعه. اقرأه بعد دقيقةٍ أو دقيقتين بـ`git pull`.

```bash
mkdir -p ops/commands
cat > ops/commands/$(date -u +%H%M)_x.json <<'J'
{"action":"dispatch","workflow":"realign_surah.yml","inputs":{
  "parent":"timings/hafs/<id>.jz","surahs":"6","skip_ms":"7840",
  "url_template":"https://.../{s:03d}.mp3","reciter_id":"<id>","riwaya":"hafs",
  "reason":"سببٌ مقيسٌ يُكتب بنصّه"}}
J
git add ops/commands && git commit -m "ops: أمرٌ" -- ops/commands && git push origin HEAD:main
```
**والأفعالُ الأربعة:**
- `{"action":"state"}` → جردٌ كاملٌ من الدلو إلى **`ops/out/state.json`** (المنشور · الهدف · كلُّ فهرسٍ فيه نقص).
- `{"action":"dispatch","workflow":"…","inputs":{…}}` → أيُّ سيرِ عملٍ مسموح.
- `{"action":"tool","tool":"index_qa/run.py","args":["--struct-only","<مفتاح>"]}` → أيُّ أداةٍ مسموحة **ومعها الدلو**.
- `{"action":"tool","tool":"ci_fleet/restore_loop.py","args":["promote"]}` → الترقيةُ بحُرّاسها.

وقوائمُ المسموح في `tools/ci_fleet/agent_cmd.py` (`ALLOWED_WF` و`ALLOWED_TOOLS`) — **وإن احتجتَ ما ليس فيها
فأضِفه وأودِعه وادفعه**، فالقائمةُ حارسٌ من الصدفة لا سجنٌ لك.

⛔ **وزنادٌ أسرعُ لحلقة الاسترجاع وحدَها:** ألحِق سطراً بـ`ops/kick.txt` وادفعه ⇒ تشتعل فوراً.
⛔ **وإن تعذّر شيءٌ حقّاً بعد المحاولة**: اكتب في `ops/out/BLOCKED.md` **ما حاولتَ والرسالةَ حرفاً وما يفتحُه**،
وادفعه، وقلهُ في تقريرك. ⭐ **«تعذّر» بلا محاولةٍ موثَّقةٍ ليست حكماً.**

## الحلقةُ التي تُديرها (كلُّ دورة)
1. **عُدّ الأسطول بالحالة لا بالصفحة، والحالاتُ ثلاث:**
   `gh api "repos/mwqwf/rafiq-align-ci/actions/runs?status=in_progress"` ثمّ `queued` ثمّ `pending`.
   ⛔ **ومَن راقب `in_progress` وحدَها أعلن تمامَ ما لم يبدأ** (وقعت فعلاً).
2. **افحص المسرحَ عن بصماتٍ جديدة:** `python tools/index_qa/bucket_watch.py --since <ISO>`.
3. **لكلّ بصمةٍ جديدة — والمجّانيُّ أوّلاً:** `python tools/index_qa/run.py --struct-only <المفتاح>`.
   فإن نجت: `openers.yml` + **أربعةُ ملوحٍ** (`audio_qa.yml` بأربعة `seed_salt` متمايزة) ثمّ `promote.py --only <المفتاح>`.
   ⛔ **ولا تُطلق ملحاً قبل الفحص البنيويّ** — أربعُ تشغيلاتٍ تضيع على فهرسٍ يردّه الحارسُ مجّاناً.
4. **حلقةُ الاسترجاع** (أنفسُ ما تعمل): `python tools/ci_fleet/restore_loop.py scan|gate|promote`
   — وهي مجدولةٌ تلقائيّاً في `restore.yml` كلَّ ثلاث ساعات. راقبها وأصلحْ ما تعثّر.
5. **وما لا يُسترجَع يُعلَن بسببٍ قِستَه** (`drop_surah.py --declare-absent` برمزٍ ورسالةٍ للمستخدم)
   — **ولا يُترك صامتاً**: سورةٌ غائبةٌ بلا بيانٍ أسوأُ من غيابٍ مُعلَن.

## ⭐ ما يجب أن تعرفه قبل أن تقيس (دروسٌ دُفع ثمنُها)
- ⭐⭐ **الدلوُ هو الحقيقة لا الوثيقة.** قائمةُ العمل تُشتقّ بالطرح من `timings/` و`catalog/reciters.json`،
  ولا تُنسخ من جدولٍ في مستند — فالجداولُ تتقادم (‏وقد أنفقتُ تشغيلةً على سورةٍ مسترجَعةٍ أصلاً).
- ⭐⭐ **المرجعُ الواحدُ يُضلّ**: قياسُ سلامة مصدرٍ بمرجعٍ واحدٍ أعطى 0.87 («مبتور») وبثلاثةٍ غيره 1.18 · 1.01 · 1.08.
  **فالحكمُ بوسيطِ أربعةِ مراجع.**
- ⭐⭐ **حين تتناقض أداتان، الحَكَمُ قياسٌ ثالثٌ من بياناتٍ تملكها** — لا ترجيحُ إحداهما بالهوى.
- ⭐⭐ **بسملةُ كلّ قارئٍ تُقاس من فهرسه نفسِه** (وسيطُ `startMs` للآية الأولى في جارات السورة)، **لا من سُلَّمٍ ثابت**:
  سُلَّمُ `basmala.yml` سقفُه 3000م.ث وبسملةُ `husary_douri` **10160م.ث** فيقول «لا بسملة» كذباً.
- ⛔ **`realign_surah` بـ`skip_ms=0` يبتلع البسملة دائماً** (الملفُّ يبدأ بها) ⇒ الاسترجاعُ **خطوتان**.
- ⛔ **`conclusion=failure` في لوحة الأشغال ليس حكماً**: الأثرُ يُقرأ من `state/` في الدلو (وقعت مراراً).
- ⛔ **`frozen_refuse` يمنع الكتابةَ لقارئٍ منشورٍ مجمَّد** ⇒ `promote.py --unfreeze <المفتاح> --reason <سبب>`
  **للحظةِ ترقيةٍ متحقَّقةٍ فقط**، والتجميدُ يعود بالترقية.
- ⛔ **«محجوزٌ بعلّةٍ مكتوبة» ليس حكماً نهائيّاً**: تُقرأ العلّةُ ويُسأل **أفي المادّة أم في أداتنا؟**
  (‏رُفع حجزان بقياسٍ فعادت الأنعامُ كاملةً عند قارئين).
- ⛔ **فهرسُ الجيل الأوّل لا يُصلحه ترقيعُ سورة** — يرث ترويسةَ أصله فيردّه حارسُ «لا أثرَ صقل» ⇒ **محاذاةٌ كاملة**.
- ⛔ **الإيداعُ بمسارٍ صريح**: `git add <مسارات>` ثمّ `git commit -m … -- <المسارات>`. **لا `-A` ولا `-a`**
  (الشجرةُ مشتركةٌ بين جلسات) · ولا `rebase`؛ وعند التعذّر `git merge --no-edit origin/main`.

## أين الحقيقة
| ماذا | أين |
|---|---|
| الحالةُ والسجلُّ التشغيليّ | `mwqwf/QuranRafiq` ← `docs/ops/RESUME_NOW.md` (اقرأ آخرَه أوّلاً) |
| السورُ المُسقطةُ وأسبابُها | `docs/qa/DROPPED_SURAHS.md` · والترقياتُ في `docs/qa/PROMOTIONS.md` |
| المنشورُ فعلاً | الدلو: `timings/**` و`timings/manifest.json` · والمسرحُ `timings-staging/**` |
| الأحكامُ الصوتيّة | الدلو: `state/**` (‏`audio-<ملح>.json` و`openers.json`) |
| الحُرّاسُ والقوائم | `tools/index_qa/` (‏`hold.txt` · `frozen.txt` · `promote.py`) |

## أدواتٌ تُعيدها كلَّ دورة
```
python tools/index_qa/low_coverage_scan.py     # سورٌ حاضرةٌ ناقصة — أخطرُ من الغائبة
python tools/index_qa/dup_sha_sweep.py         # بصمتا صوتٍ لسورتين (تحريفٌ سمعيّ)
python tools/index_qa/triage.py --brief        # حصيلةُ المنشور
```

## كيف تُنهي دورتَك
**لا تنتهِ بلا:** (١) إيداعٍ بمسارٍ صريحٍ ودفعٍ إلى `main`، (٢) سطرٍ في `docs/ops/RESUME_NOW.md`
يقول **ما قِيس وما تغيّر وما بقي**، (٣) **جدولةِ يقظةٍ أو تشغيلةٍ تُكمل بعدك**.
وإن أُنجز كلُّ شيءٍ فاكتب ذلك صريحاً — «لا شيءَ باقٍ» حكمٌ يحتاج جرداً من الدلو لا انطباعاً.
