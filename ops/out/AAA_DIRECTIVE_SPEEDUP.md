# ⚡ تكليفٌ عاجلٌ لمناوبة الفهرسة — يُقرأ قبل كلّ شيء (المشرف · 2026-09-14 12:2xZ)

> ⛔ **أُرسل هذا أوّلاً في المسألة 1 (تعليق `5663009745`) فلم يُلتقط** — ودورةُ 11:19Z مرّت
> بلا أثرٍ له. فكُرِّر **هنا** لأنّ `ops/out/` مقروءٌ في كلّ دورةٍ يقيناً. اقرأه ونفّذه.

## العلّةُ المقيسة: التوازي مسقوفٌ بعدد القرّاء لا بعدد السور
قِيس على الشوط `34813198468` (`align.yml` · `shards=10` · `reciters_gen1_fix10.tsv` بـ11 صفّاً):
- المصفوفةُ `rows[i::shards]` ⇒ **الوحدةُ الذرّيّة = القارئ**: شريحةٌ واحدة = **مصحفٌ كاملٌ (114 سورة) بالتسلسل على عدّاءٍ واحد**.
- **أربعُ شرائحَ من العشر أنهت «تشغيلَ السائق» في ثانيتَين** (خاناتٌ فارغة) ⇒ رفعُ `shards` **لا يسرّع شيئاً**؛ السقفُ `min(shards, rows)`.
- والمصحفُ الكاملُ يتجاوز `timeout-minutes: 350` ⇒ يُقطع ويُستأنف موجةً بعد موجة — **وهنا تضيع الأيام، لا في المحاذاة**.
- و`concurrency.group … 'full'` يُسلسل الموجات ⇒ **لا علاجَ بموجةٍ ثانيةٍ متوازية؛ العلاجُ داخلَ الموجة**.

## العلاج: التقسيمُ بـ(قارئ × مدى سور)
صوتُ كلِّ سورةٍ ملفٌّ مستقلٌّ أصلاً (`{surah:03d}.mp3`) ⇒ التقسيمُ **لا يمسّ المحاذاةَ بحرفٍ**.
11 قارئاً × 4 أجزاء = **44 وحدة** ⇒ توازٍ **20** بدل 10، **وكلُّ وحدةٍ داخلَ السقف فلا قطعَ ولا إعادةَ موجات**.

## شروطٌ لا تُخالَف
1. **الدمجُ هو الموضعُ الخطر لا التقسيم**: لا ترقيةَ إلا بـ**114/114** وعدِّ آياتٍ كامل. جزءٌ ناقصٌ ⇒ لا شيءَ يُنشر.
2. مفتاحُ الكاش يتبع **(القارئ + مدى السور)** لا رقمَ الخانة (درسُ 2026-09-12: ساعاتٌ ضاعت).
3. **جرِّبها على قارئٍ واحدٍ أوّلاً** وقارن فهرسَه بفهرسِ مسارٍ غيرِ مقسَّم — **فإن اختلف حرفٌ يُسحب التقسيم**.
4. ⛔ **ولا عتبةَ تُخفَّض ولا حارسَ يُعطَّل ولا بوّابةَ تُقصَّر لأجل السرعة.** شرطُ المالك: **لا تحريفَ للقرآن**، والسرعةُ تُشترى من التوازي وحدَه.

📌 مأذونٌ فيه سلفاً (المستودعُ العامُّ المجّانيّ · قابلٌ للرجوع · لا يمسّ منشوراً). وإن تعذّر فاكتب نصَّ الخطأ في `ops/out/BLOCKED.md` وامضِ.
