# rc=99  (لم يُنفَّذ — سُحب الأمرُ يدويّاً لأنّه كان يحجز الطابور)
⛔ `_debug_state_for.py qalun/akri_qalun` يسرد `state/` كلَّه ويقرؤه بلا ذاكرةِ
   ETag (بخلاف `promote.bucket_reports`) ⇒ يتجاوز `timeout-minutes: 30`
   فيُلغى الشوطُ **قبل أن يُنفَّذ أيُّ أمرٍ بعده**، ثمّ يُعاد الكرّةَ أبداً.
   وقد حجز الطابورَ (عشرةُ أوامر) نحوَ نصفِ ساعةٍ في الشوطين 235 و236.
⇒ سُحب، وتُطلب بصمةُ akri_qalun بطريقٍ أرخص: فحصٌ بنيويٌّ للمنشور يكتب تقريراً
  في `state/`، ثمّ `dump_state.py` على ذلك التقرير وحدَه (قراءةُ كائنٍ واحد).
