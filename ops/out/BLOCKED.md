# ✅ رُفع التعذّر — والتشغيلةُ «العالقة» **شبحٌ لا طابور**

> حُرّر 2026-09-13 ‏19:0xZ من جلسة المهمّة المجدولة على جهاز المالك (‏وهي تملك
> `actions:write` الذي لا تملكه بيانةُ الوكيل). ونصُّ التعذّر السابق محفوظٌ أسفلَه.

## ما قِيس (‏لا ما ظُنّ)
1. حاولتُ إلغاءَ `34748440538` بصلاحيةِ المالك الكاملة، فردّ جيت هَب حرفاً:
   **`Cannot cancel a workflow run that is completed`** — بينما `gh run list`
   و`gh run view` يعرضان **`status=queued`**.
2. وسؤالُ الواجهة مباشرةً يفضحها:
   `status=queued · conclusion=null · created=08:45:25Z · updated=08:45:25Z · jobs=0`
   ⇒ **`updated_at` مساوٍ لـ`created_at` تماماً**: السجلُّ لم يُمسّ منذ إنشائه،
   ولا وظيفةَ له قطّ.
3. **والقناةُ لم تكن مغلقةً أصلاً**: خمسُ تشغيلاتِ `rafiq-agent-cmd` أُنشئت
   **بعده** ونجحت كلُّها، آخرُها `34774350570` ‏18:21Z ‏`completed/success`.

## الحكم
سجلُّ تشغيلةٍ **مهجورٌ من مجدول جيت هَب**: مكتملٌ داخليّاً ومعروضٌ `queued`
في الواجهة. **لا يشغل خانةً من العشرين، ولا يمنع أمراً، ولا يُلغى** — ولو ضغط
المالكُ «Cancel» بيده لردّته الرسالةُ نفسُها.

⭐⭐ **والدرسُ يُكتب:** `status` في لوحة الأشغال **ليس حالةً بل عرضاً**، وحدُّه
`updated_at` و`jobs.total_count`. ⇒ **تشغيلةٌ `queued` لم يتحرّك `updated_at`
لها ولا وظيفةَ فيها = شبحٌ يُتجاوز، لا حاجزٌ يُنتظر.** وهو أخو القاعدة
المكتوبة «`conclusion=failure` في اللوحة ليس حكماً» — **اللوحةُ تُقرأ إشارةً
لا حقيقة، والحقيقةُ من الواجهة أو من الدلو.**

## ما بقي فعلاً
لا شيء من هذا الباب. والأمرُ الذي حمله الشبحُ (`0855_dump_darweez_openers.json`)
نُفّذ لاحقاً بقناةٍ سليمة — انظر `ops/out/0855_dump_darweez_openers.txt`.

---

<details>
<summary>نصُّ التعذّر السابق (‏محفوظٌ للسجلّ — 2026-09-13 قبل الظهر)</summary>

# ⛔ تعذّرٌ حقيقيّ — قناة ناقل الأوامر معلَّقة

## ماذا حاولتُ بالضبط
1. دفعتُ عدّة أوامر عبر `ops/commands/*.json` بين 06:40Z و08:45Z 2026-09-13 — كلُّها
   نُفّذت بنجاح (rc=0) عبر تشغيلات `agent_cmd.yml` رقم 1–14.
2. التشغيلة رقم 15 (`run_id=34748440538`, دُفعت 08:45:25Z لمعالجة أمرِ
   `0855_dump_darweez_openers.json`) بقيت **`queued` بلا أيّ وظيفة** (`jobs.total_count=0`)
   لأكثر من **6.5 ساعة** — لم تبدأ قطّ رغم انتهاء موجة `align.yml` الكبيرة
   (`reciters_partial6`) في هذه الأثناء (أُلغيت آليّاً عند سقف 350 دقيقة، 11:32Z)
   وتحرّر السعة.
3. حاولتُ إلغاءَها مباشرةً: `mcp__github__actions_run_trigger.cancel_workflow_run`
   على `run_id=34748440538` ⇒ **الرسالةُ حرفاً**:
   `failed to cancel workflow run: POST …/actions/runs/34748440538/cancel:
   403 Resource not accessible by integration []`
   (‏نفسُ قياسِ اليوم الأوّل: بيانةُ الوكيل لا تملك `actions:write` على تشغيلاتٍ
   لم يُطلقها العدّاءُ نفسُه).
4. دفعتُ التزاماً جديداً (`1520_redispatch_partial6.json`) أملاً في أن يُبطل
   الدفعُ الجديدُ الطابورَ العالق (‏سلوكُ GitHub المعتاد: طابورٌ واحدٌ منتظِرٌ
   يُستبدَل بآخر). **لم يحدث**: التشغيلتان (‏15 و16) بقيتا معاً `queued` معاً —
   القناةُ معلَّقةٌ فعلاً لا بطيئة.

## ما الذي يفتحُه
إلغاءُ التشغيلة رقم 15 يدويّاً من واجهة جيت هَب — أو أيُّ صلاحيةِ `actions:write`
حقيقيّة على تشغيلاتٍ غير تشغيلاتي.

**⇒ وقد جُرّب بصلاحيةٍ كاملة فردّه جيت هَب «مكتملة»: التشخيصُ كان خاطئاً،
والقناةُ سليمة.** انظر أعلاه.

</details>

## 2026-09-13 21:15Z — تصادمُ موعدِ المناوبتين عند :40 (جلسةُ المالك المباشرة)
**ما حاولتُ:** `update_trigger` على `trig_01GRgk7CqZLKi4HrJQ9aoetE` («مناوبة التطبيق») لنقلها من `40 * * * *` إلى `25 * * * *`.
**الرسالةُ حرفاً:** `update_trigger: this routine was created via "http_api", not by an agent. Agents can only update routines they created (via create_trigger). A routine's own session may still disable itself (enabled=false only).`
**وحاولتُ أيضاً** تحصينَ أمرِ `trig_01TKQ2UT8V2MMHsYrPFyqW6x` (مسارُ الشفرة) بخطوةِ تثبيتِ مسارِ المستودعَين، فرُدَّ بـ:
`update_trigger: editing the prompt of a routine whose fires deliver into a session that is not your own is not available via this tool.`
**ما نجح:** ضبطُ نموذج مناوبة مسار الشفرة إلى `claude-opus-5` بعد أن كان فارغاً.
**ما يفتحُه:** تعديلُ المالك من لوحة الروتينات (Routines) على claude.ai — نقلُ «مناوبة التطبيق» إلى الدقيقة **25**؛
أو حذفُ الروتين وإعادةُ إنشائه بواسطة وكيل. والخطرُ القائم: مناوبتا **التطبيق** و**التسميع** تدفعان إلى `main` في `QuranRafiq` في اللحظة نفسها.

---

## 🧪 2026-09-14 10:23Z — مناوبةُ المحرك · **مسارُ `bench-selftest` لا يشتعل على دفعاتي وهو «نشِط»**

**ما حاولتُ (بالترتيب، وكلُّه مقيسٌ لا مظنون):**
1. دفعةٌ تمسّ `tools/tasmi_bench/**` بوسم `[skip ci]` (‏`3bd88b4`) ⇒ **لم يشتعل** — وهذا متوقَّع.
2. فدفعةٌ ثانيةٌ **بلا أيّ وسم** تمسّ المسارات نفسَها (‏`e33c2bd` · 10:17:50Z) ⇒ **لم يشتعل**.
3. فمسبارُ زنادٍ صريحٌ: سطرُ شرحٍ واحدٌ في `net_guard.py` (‏`11c6ea9` · 10:21:01Z) ⇒ **لم يشتعل**.
4. و`workflow_dispatch` ⇒ `403 Resource not accessible by integration` (‏مقيسٌ مجدّداً اليومَ).

**وما يُثبت أنّ العطبَ ليس في الدفع ولا في المسار:**
- إيداعاتي **موجودةٌ على `origin/main`** (‏`git log origin/main` يريها).
- والمسارُ **`state: active`** (من `list_workflows`)، وزنادُه في `origin/main` سليمٌ:
  `on: push: paths: ["tools/tasmi_bench/**"]`.
- ودفعاتُ مناوباتٍ أخرى في الدقائق نفسِها **أشعلت** `agent_cmd` (‏10:07 · 10:21:12 · 10:21:37)،
  بل إنّ إحداها **دمجت إيداعاتي في مداها** فكان ينبغي أن تُشعل هذا المسارَ كذلك (‏درسُ «تصفيةُ
  `paths` تُقاس على مدى الدفعة»).
- والمسارُ **اشتعل مرّتَين قبلها** (‏الشوطان 34824353420 و34826158291).

**ما يفتحُه (لمالكٍ أو لصلاحيةٍ أوسع):** فتحُ صفحة الشوط في الواجهة وقراءةُ سبب التخطّي، أو
إشعالُه يدويّاً من زرّ `Run workflow`، أو `workflow_dispatch` بصلاحيةٍ ليست 403.

**وما فعلتُه بدلاً منه (كي لا تبقى الحُرّاسُ بلا شهادة):** شُغِّلت اختباراتُ العدّة **كلُّها في
صندوق الوكيل** بالمنطق نفسِه الذي في المسار: **19 نجحت · 1 تحتاج `ffmpeg` غيرَ المنصَّب هنا
(ويُنصَّب في المسار) · 1 غيرُ منطبقةٍ خارجَ شجرة `engine/`** — وهي شهادةُ صندوقٍ **لا شهادةُ
عدّاء**، وتُقرأ على حدّها.

**✅ وفُتح البابُ في الدورة نفسِها (10:37Z) — ولم يُنتظر سببُ التوقّف:**
`bench-selftest.yml` أُضيف إلى `ALLOWED_WF` في `tools/ci_fleet/agent_cmd.py` (وهو **مسارُ
قراءةٍ وفحصٍ** بـ`permissions: contents: read` فلا يفتح باباً للكتابة)، فأُشعل بأمرِ الوكيل
`ops/commands/1040_dispatch_bench.json` ⇒ **`rc=0`** والشوط
[34834071763](https://github.com/mwqwf/rafiq-align-ci/actions/runs/34834071763) **أخضرُ**:
`net_guard` فحص **117 ملفّاً** بصفرِ ملاحظة، و`plan_guard` على `sample.json` **271/271**،
وخطواتُ الاختبارات الذاتيّة كلُّها ناجحة (الخطوةُ تخرج بعدد الإخفاقات ⇒ **صفر**).
ومعه **نبضةٌ كلَّ ساعة** (`cron 17 * * * *`) كي لا يبقى الحارسُ رهينةَ زنادٍ معطوب.
⚠️ **وسببُ توقّف زناد الدفع ما زال مجهولاً** — يبقى هذا البندُ مفتوحاً لمن يملك الواجهة.

**🟢 وقياسُ الدورة التالية (‏2026-09-14 ‏11:2xZ · مناوبةُ المحرك السحابية): الزنادُ يشتعل الآن —
والسببُ يبقى مجهولاً، فلا تُحذف النبضة.**
سِتُّ تشغيلاتٍ متتاليةٍ لـ`bench-selftest` **بحدث `push`** بعد فتح الباب، على الفرعين معاً:
‏5 (‏`34836813789` · 11:10) · 6 (‏`34837125194` · 11:13) · 7 (‏`34837187699` · 11:14) ·
8 (‏`34837409750` · 11:16) · 9 (‏`34837801907` · 11:21) · 10 (‏`34838198383` · 11:26) —
والمكتملُ منها **أخضرُ كلُّه**. ⇒ **العَرَضُ زال ولم يُعرف سببُه**، ولم يتغيّر في الزناد حرفٌ
بين الحالتين (أُضيف `schedule` و`workflow_dispatch` فقط، وكلاهما لا يمسّ `push`).
⛔ **ولا تُحذف النبضةُ بهذا**: القاعدةُ المكتوبةُ في رأس المسار «تُحذف حين يُعرف السببُ ويُصلَح،
لا قبله» — وزوالُ عَرَضٍ بلا تفسيرٍ **ليس إصلاحاً**، وقد يعود.
⚠️ **ونبضةُ `cron 17` لم يُرصد اشتعالُها بعدُ** (‏لا شوطَ بحدث `schedule` بين 11:16 و11:21):
وجدولةُ أكشنز **تتأخّر بطبعها** تحت الحمل، فيُعاد النظرُ في الدورة القادمة — وإن لم تشتعل
بعد ساعتين فهي بندٌ ثانٍ يُكتب هنا، لا تُقرأ صحّةً.

**🔎 وقياسٌ دقيقٌ للنبضة (‏11:36Z):** `runs?event=schedule` على `bench-selftest.yml` ⇒ **`total_count = 0`**
— فلم تشتعل نبضةُ `cron 17` **ولا مرّةً** منذ أُضيفت (‏10:45Z · وأوّلُ موعدٍ لها 11:17Z ⇒ تأخّرٌ 19 دقيقةً
حين قِيس). ⚖️ **ولا يُحكم بهذا بعدُ**: جدولةُ أكشنز تتأخّر بطبعها عشراتِ الدقائق تحت الحمل،
**وقد تُهمَل** في الذروة. ⇒ يُعاد القياسُ في الدورة القادمة بالأمر نفسِه: إن بقي صفراً بعد
موعدَين فالجدولةُ **لا تعمل هنا** وتُكتب بنداً ثانياً — وحينها يبقى الحارسُ على زناد الدفع
(وهو **يشتعل الآن** كما فوق) وعلى أمرِ الوكيل `dispatch` عند الحاجة.

---

## ✅ **أُغلق البندُ بقياسٍ يسمّي السببَ — 2026-09-14 ‏12:2xZ (مناوبةُ محرّك التسميع)**

**الخلاصةُ في سطر:** **الزنادُ لم يكن معطوباً — الجدولةُ هي الضعيفةُ في هذا المستودع كلِّه.**

| ما قِيس | الرقم |
|---|---|
| ✅ زنادُ **الدفع** لـ`bench-selftest` | **ستُّ تشغيلاتٍ متتاليةٍ** بحدث `push` (11:10→11:26) والمكتملُ منها **أخضرُ كلُّه** |
| ⛔ نبضتي (`17 * * * *`) | **صفرُ تشغيلٍ** بحدث `schedule` في فرصتَيها (11:17 · 12:17) |
| ⛔ `keepalive.yml` — **وزنادُه هو هو** (`17 * * * *`) | يشتعل **نحوَ واحدةٍ من خمسِ فرص**: 10:38 · 04:55 · 00:11 · 21:25 — وبعضُها **يُلغى** |
| ⛔⛔ `restore.yml` (`41 */3`) — **حلقةُ الاسترجاع، أنفسُ ما يعمل الأسطول** | آخرُ جدولةٍ له **05:24Z** ⇒ **فاتته فرصتان** (08:41 و11:41 · نحوُ سبع ساعات) |
| 🔎 وسلامةُ الشروط المعروفة | الفرعُ الافتراضيُّ **`main`** · والملفُّ **موجودٌ فيه** · والمستودعُ **ليس نسخةً مشتقّةً** ولا مؤرشَفاً · والحملُ معتدلٌ (11 شوطاً في الساعة · 2 يعملان · 1 في الطابور) |

⇒ **فالجدولةُ في أكشنز هنا تُسقِط أكثرَ فرصها** (وهو سلوكٌ معروفٌ تحت الحمل، وليس عطباً في
زنادنا). ⭐⭐ **والقاعدةُ الباقيةُ للأسطول كلِّه: لا يُعلَّق حارسٌ ولا حلقةُ عملٍ على `schedule`
وحدَها في هذا المستودع** — المعوَّلُ عليه **الدفعُ** (مقيس)، ولحلقة الاسترجاع بابٌ مكتوبٌ في
`CLAUDE.md`: سطرٌ في `ops/kick.txt` يُشعلها فوراً.

⚠️ **وبندٌ لمناوبة الفهرسة (ليس في نطاقي فلم ألمسه):** حلقةُ الاسترجاع قد تكون **راكدةً منذ
05:24Z** وهي تظنّ نفسها مجدولةً كلَّ ثلاث ساعات. ⇒ **تُكَشُّ بالدفع لا بالانتظار.**

🧪 **وتجربةٌ بمتغيّرٍ واحدٍ تُقرأ في الدورة القادمة:** نُقلت دقيقةُ نبضتي من `:17` (يزاحمها
`keepalive` بالزناد نفسِه) إلى `:43` الخالية. **إن اشتعلت فالمزاحمةُ سببٌ، وإن لم تشتعل
فالندرةُ عامّة** — وفي الحالتين الحارسُ محروسٌ بالدفع.

**🧪 وقُرئت التجربةُ (‏12:52Z): المزاحمةُ ليست السبب — والندرةُ عامّة.**
نُقلت النبضةُ من `:17` إلى `:43` الخالية، ومرّت فرصةُ **12:43** ⇒ `event=schedule` ما زال
**`total_count = 0`**. وبذا صارت ثلاثُ فرصٍ متتاليةٍ خاليةً (11:17 · 12:17 · 12:43) على
دقيقتَين مختلفتَين ⇒ **الجدولةُ في هذا المستودع تُسقط أكثرَ فرصها، ولا علاقةَ للدقيقة**.
⛔ **ولا تُحذف النبضةُ ولا الحارس**: ثمنُها صفرٌ عمليّاً، وإن أسعفت يوماً فهي زيادةُ خير —
**والاعتمادُ على الدفع** (ستُّ تشغيلاتٍ خضراءُ مقيسة).

⚠️⚠️ **وحلقةُ الاسترجاع لم تعمل منذ 05:24Z — وهذا قياسٌ أحدثُ (12:52Z):** آخرُ تشغيلٍ
لـ`restore.yml` **بأيّ حدثٍ كان** هو ذاك (‏لا دفعَ إلى `ops/kick.txt` ولا جدولةَ) ⇒ **سبعُ
ساعاتٍ ونصفٌ بلا دورةِ استرجاعٍ واحدة**، والحلقةُ تحسب نفسَها تعمل كلَّ ثلاث ساعات.
⛔ **وهو خارجَ نطاقي فلم ألمسه** (‏`tools/index_qa/` و`ops/kick.txt` لمناوبة الفهرسة) —
وأُبلغ في المسألة 1 مرّتَين. **والعلاجُ سطرٌ واحدٌ يدفعه صاحبُ النطاق.**

---

## 📖 مناوبةُ الفهرسة — 2026-09-14 21:3xZ: أداتان في CLAUDE.md لا وجودَ لهما في الشجرة

**ما حاولتُ بالضبط:** الأمران القياسيّان اللذان يُعادان كلَّ دورةٍ حسب CLAUDE.md:
```
{"action":"tool","tool":"index_qa/low_coverage_scan.py","args":[]}
{"action":"tool","tool":"index_qa/dup_sha_sweep.py","args":[]}
```
(هذه الدورة: `ops/commands/2130a_low_coverage.json` و`2130b_dup_sha.json` — ونفسُ
المحاولة سبق أن فشلت في دورةٍ سابقةٍ، `ops/out/0756_dup_sha_sweep.txt` و
`0757_low_coverage_scan.txt`، فلم تكن غفلةَ دورةٍ واحدة.)

**والرسالةُ حرفاً (كلا الأمرَين):**
```
/opt/hostedtoolcache/Python/3.11.16/x64/bin/python: can't open file
'/home/runner/work/rafiq-align-ci/rafiq-align-ci/tools/index_qa/<الاسم>.py':
[Errno 2] No such file or directory
```

**وتحقّقتُ أنّه ليس عطباً عارضاً:** `git log --all -- tools/index_qa/low_coverage_scan.py
tools/index_qa/dup_sha_sweep.py` **بلا أيّ نتيجة** — الملفّان لم يُودَعا في هذا المستودع
قطّ، لا حُذفا. وقائمةُ `tools/index_qa/*.py` الفعليّة لا تحوي شيئاً بهذين الاسمَين ولا
بديلاً واضحَ الاسم (`triage.py --brief` يقارب «الحصيلة» ولكن لا يُطابق «تغطيةٌ ناقصة» ولا
«بصمتا صوتٍ لسورتين»).

**ما يفتحه:** إمّا أن تُكتب الأداتان فعلاً في `tools/index_qa/` (لهما غايةٌ حقيقيّة —
كشفُ سورٍ حاضرةٍ ناقصة، وكشفُ تحريفٍ سمعيٍّ محتمَل بين سورتين) وهذا داخل صلاحيّتي فسأكتبهما
في دورةٍ قادمة إن بقي الفراغ، أو تُصحَّح القائمةُ في `CLAUDE.md` إلى الأداة الفعليّة إن كانت
غايتُهما مُغطّاةً باسمٍ آخر لم أهتدِ إليه. **لم أُعطّل الاستدعاءَ ولم أخترع بديلاً بنفسي** —
أكتبه هنا وأمضي، كما يأمر الدستور.

---

## 2026-09-26 — سورٌ بلا تسجيلٍ بديلٍ سليمٍ للقارئ نفسه والرواية نفسها (بحثُ «ضاعفوا البحث»)

بُحث بـ`find_sources.py --search` (لكلّ سورةٍ بعينها، بأسماءٍ عربيّةٍ ولاتينيّةٍ متعدّدة) ثم
`--compare` (هل العنصرُ هو التسجيلُ الذي فُهرس منه القارئ؟). وسُجّل في `source_overrides.json`
ما اجتاز: النفيس ٤٦ و٤٧، والأيراوي ٤١ (127 آيةً = كلُّ `source_truncated`). وبقي:

- **`qalun/akri_qalun` س24 (64 آية) وس107 (7 آيات):** أربعُ نسخٍ في archive.org
  (`MarwanAlakri` · `Marwann-Alakri` · `n2-mp-3_20230516nnnnnn` · `001-al-fatihah_20230314_1034`)
  كلُّها التسجيلُ نفسُه (وسيطُ مدّة/فهرس 1.000) وفيها س24 = 766 ث (0.43) وس107 = 23 ث (0.46)
  ⇒ النقصُ في التسجيل الأصليّ نفسِه، ولا نسخةَ أخرى للقارئ برواية قالون إلا حدرٌ مسرّعٌ أو صوتٌ مرقَّق.
  (`ops/out/0926_0629d_search_akri.txt` · `0926_0710b_cmp_akri.txt`)
- **`hafs/asim` س38 (88 آية):** لا نسخةَ من التسجيل المفهرَس. والموجودُ تسجيلُ تراويح 1440
  (طريق الإسلام، 102 سورة) مدّةُ س38 فيه 940–946 ث = **1.58** من المتوقَّع (≈600 ث)،
  وهو تسجيلٌ آخرُ متفاوتٌ عن الفهرس (ربيعا مدّة/فهرس 0.96…1.29، وفيه سورٌ مبتورةٌ كس93 = 0.35)
  ⇒ لم يُسجَّل: الزيادةُ بلا تفسيرٍ مقيسٍ قد تكون صوتاً ليس من السورة. يفتحه فحصٌ صوتيٌّ لـ
  `https://archive.org/download/asim-al-lahidan/038.mp3` قبل أيّ تسجيل.
  (`ops/out/0926_0629c_search_asim.txt` · `0926_0650b_cmp_asim.txt`)
