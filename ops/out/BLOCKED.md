# ⛔ تعذّرٌ حقيقيّ — قناة ناقل الأوامر معلَّقة

## ماذا حاولتُ بالضبط
1. دفعتُ عدّة أوامر عبر `ops/commands/*.json` بين 06:40Z و08:45Z 2026-09-13 — كلُّها
   نُفّذت بنجاح (rc=0) عبر تشغيلات `agent_cmd.yml` رقم 1–14.
2. التشغيلة رقم 15 (`run_id=34748440538`, دُفعت 08:45:25Z لمعالجة أمرِ
   `0855_dump_darweez_openers.json`) بقيت **`queued` بلا أيّ وظيفة** (`jobs.total_count=0`)
   لأكثر من **6.5 ساعة** — لم تبدأ قطّ رغم انتهاء موجة `align.yml` الكبيرة
   (`reciters_partial6`) في هذه الأثناء (أُلغيت آليّاً عند سقف 350 دقيقة، 11:32Z)
   وتحرّر السعة.
3. حاولتُ إلغاءَها مباشرةً: `mcp__github__actions_run_trigger.cancel_workflow_run`
   على `run_id=34748440538` ⇒ **الرسالةُ حرفاً**:
   `failed to cancel workflow run: POST …/actions/runs/34748440538/cancel:
   403 Resource not accessible by integration []`
   (‏نفسُ قياسِ اليوم الأوّل: بيانةُ الوكيل لا تملك `actions:write` على تشغيلاتٍ
   لم يُطلقها العدّاءُ نفسُه).
4. دفعتُ التزاماً جديداً (`1520_redispatch_partial6.json`) أملاً في أن يُبطل
   الدفعُ الجديدُ الطابورَ العالق (‏سلوكُ GitHub المعتاد: طابورٌ واحدٌ منتظِرٌ
   يُستبدَل بآخر). **لم يحدث**: التشغيلتان (‏15 و16) بقيتا معاً `queued` معاً —
   القناةُ معلَّقةٌ فعلاً لا بطيئة.

## ما الذي يفتحُه
إلغاءُ التشغيلة رقم 15 (`https://github.com/mwqwf/rafiq-align-ci/actions/runs/34748440538`)
يدويّاً من واجهة جيت هَب (زرّ Cancel workflow) — أو أيُّ صلاحيةِ `actions:write`
حقيقيّة على تشغيلاتٍ غير تشغيلاتي. بعدها ستُستأنف القناةُ فوراً (الأوامرُ
المتراكمة في `ops/commands/` ستُنفَّذ بترتيبها بمجرّد أن يحصل عدّاءٌ حرٌّ على دور).

## الأثر — ولا شيءَ منه يمسّ سلامةَ المصحف
- **الحلقةُ الأساسيّةُ (`restore.yml`) سليمةٌ وتعمل بجدولها المستقلّ** — آخرُ
  تشغيلةٍ (12:15–12:30Z) نجحت بمعزلٍ تامٍّ عن هذه القناة.
- **موجتا `align.yml`** (`reciters_gen1_fix10` تعمل الآن · `reciters_partial6`
  بانتظار دورها بعد إعادة دفعي) **لا تعتمدان على ناقل الأوامر** — أُطلقتا به،
  لكنّهما تشغيلاتٌ مستقلّة بمجرّد الإدلاء.
- **المتأثّر فقط**: أوامر تشخيصٍ وقراءةٍ إضافيّة دفعتُها (‏عرض تقرير مطالع
  `darweez`، إعادة إطلاق `partial6`) ستنتظر حتى تُفتح القناة. لا ترقيةَ ولا
  محاذاةَ عالقةً بسبب هذا.

## الخطوةُ التالية
أعلمتُ المالك في تقريري. إن رأى هذا الملفَّ ولم يُلغِ التشغيلةَ العالقةَ يدويّاً،
فسأحاول ثانيةً في الدورة القادمة — والانتظارُ آمنٌ، فالفشلُ هنا وقتٌ لا فسادُ بيانات.
