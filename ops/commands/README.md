# صندوقُ أوامر الوكيل السحابيّ

ضع ملفَّ `‎*.json` هنا وادفعه ⇒ ينفّذه العدّاءُ **بصلاحيّاته** ويردّ الجوابَ في `ops/out/<الاسم>.txt`.
ثمّ يُنقل الأمرُ إلى `done/` فلا يُعاد.

⛔ ولا يُنفَّذ إلا ما في قائمة المسموح داخل `tools/ci_fleet/agent_cmd.py`.
